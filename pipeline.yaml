version: "2.0"

dates:
  mode: rolling
  rolling:
    window: 365
    start: "2025-12-03"

parallelism:
  fetch: 4
  build: 4

settings:
  network: mainnet
  timezone: UTC
  flagship_chart_id: "region-winner-grouped-bar"
  data_dir: "build/data"
  dist_dir: "site/dist"
