Distribution of per-slot column spread: the difference between the maximum and minimum first-seen latency across all 128 columns for each slot.
A narrow distribution indicates that all columns tend to propagate at roughly the same speed within each slot.
A fat right tail suggests that some slots have one or more columns that lag significantly behind the rest.
This histogram aggregates the entire day; use the spread timeseries to see how spread evolves over time.
