Median first-seen latency per (column index, 5-minute bucket) pair across the day.
Darker cells indicate faster propagation; lighter cells indicate higher latency.
Columns that are structurally slower (e.g., due to position in the erasure code) will show consistently lighter bands.
Use the delta and normalized views to isolate time-of-day effects from structural column differences.
