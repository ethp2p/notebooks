Per-slot column spread plotted over the day: each point is the max minus min first-seen latency across all 128 columns for that slot.
High spread at a particular time may coincide with network events, epoch boundaries, or block reward anomalies that delay specific custodians.
Persistent elevated spread throughout the day points to structural custody-set imbalances rather than transient events.
Pair with the spread histogram to understand whether observed spikes are typical or outliers.
