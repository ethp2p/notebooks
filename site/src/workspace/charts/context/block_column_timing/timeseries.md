Per-slot spread between block arrival and last column seen, plotted over the day. Each point represents one slot; color encodes blob count.
Temporal clustering of high-spread slots may coincide with epoch boundaries, network events, or periods of elevated blob volume.
Slots with zero or very few blobs tend to cluster near the bottom; slots with many blobs trend higher, which can be confirmed by filtering the visualMap range.
Pair with the histogram to understand whether elevated periods are typical or statistical outliers for the day.
