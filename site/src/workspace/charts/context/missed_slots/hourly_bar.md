Bar chart showing the count of missed slots grouped by UTC hour across the
selected day. Peaks in specific hours may indicate network congestion, relay
outages, or scheduled maintenance windows. All 24 hours are always rendered,
with zero counts shown as empty bars.
