Horizontal bar chart showing the miss rate (percentage of bid slots with no
block arrival) for each entity. Only entities with at least 5 bid slots are
included to reduce noise from low-volume participants. Color transitions from
teal (low rate) to amber (high rate) to highlight severity.
