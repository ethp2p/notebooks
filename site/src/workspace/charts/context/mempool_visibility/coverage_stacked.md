For each transaction type, the bar shows how many transactions were seen in the mempool before inclusion (before), after (after), or never seen (never).
A large "before" segment indicates healthy mempool visibility; a large "never" segment suggests transactions are being included from private order flow or builder bundles that bypass public mempool propagation.
