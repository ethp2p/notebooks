Each point is one slot for the selected day; x is UTC time and y is the number of blobs included in that slot.
Slots with zero blobs show at the bottom of the chart; a dense band near the maximum target (6) indicates healthy blob utilization.
Color intensity tracks blob count, making high-density regions visually distinct even at large scales.
