The day is divided into four equal time windows, each showing a heatmap of blob count per slot-in-epoch for its window.
Splitting into facets preserves per-slot resolution that would be lost if all 225 epochs were compressed into a single chart.
Comparing the four panels side-by-side reveals whether blob utilization patterns shifted across different parts of the day.
