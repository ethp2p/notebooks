Each column is one epoch; the 32 rows represent slot positions within that epoch (slot 0 at top).
Color intensity encodes the blob count for that specific slot, making periodic patterns within epochs visible as horizontal bands.
Persistent dark rows at the same slot position across many epochs may indicate a systematic inclusion advantage for certain slot indices.
