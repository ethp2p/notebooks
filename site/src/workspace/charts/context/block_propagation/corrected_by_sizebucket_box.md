Horizontal grouped boxplot of size-corrected latency, one group per wire-size
bucket. Comparing bucket distributions reveals whether routing overhead is
constant across block sizes or grows with payload.
