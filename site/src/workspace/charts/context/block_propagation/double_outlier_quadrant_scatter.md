Scatter with two threshold lines defining four quadrants: normal, large-only,
slow-only, and double-outlier (both large and slow). The double-outlier
quadrant is the primary diagnostic zone for problematic block propagation.
