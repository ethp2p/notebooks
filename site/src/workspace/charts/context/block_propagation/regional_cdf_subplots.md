CDF of block propagation latency broken out by region and observation source
(sentry vs contributoor). Each subplot holds four lines, one per wire-size
bucket. Steeper slopes indicate tighter latency distributions in that region.
