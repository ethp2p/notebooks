Two 2D density heatmaps (MEV and local builders) showing the joint
distribution of wire size and size-corrected latency. High-density bands
at specific latency levels indicate builder infrastructure clusters.
