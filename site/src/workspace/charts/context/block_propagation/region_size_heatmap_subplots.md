Two heatmaps (sentry and contributoor) showing median propagation latency
as a function of region (rows) and wire-size bucket (columns). Color intensity
maps directly to latency; darker cells mean slower propagation.
