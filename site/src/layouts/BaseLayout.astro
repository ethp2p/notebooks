---
import '../styles/global.css';
import Sidebar from '../components/Sidebar.astro';
import TableOfContents from '../components/TableOfContents.astro';

interface Props {
  title: string;
  description?: string;
  showToc?: boolean;
}

const { title, description = 'Ethereum P2P network analysis notebooks', showToc = false } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#f8f7fc" id="theme-color-meta" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | Eth P2P Notebooks</title>

    <!-- Prevent flash of wrong theme -->
    <script is:inline>
      (function() {
        const stored = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;

        let isDark;
        if (stored === 'dark') isDark = true;
        else if (stored === 'light') isDark = false;
        else isDark = prefersDark; // 'system' or not set

        if (isDark) {
          document.documentElement.classList.add('dark');
          document.getElementById('theme-color-meta')?.setAttribute('content', '#1a1625');
        }
      })();
    </script>
  </head>
  <body class="min-h-screen">
    <div class="layout-container flex min-h-screen">
      <!-- Left Sidebar -->
      <aside class="left-sidebar fixed top-0 left-0 h-screen w-64 bg-sidebar border-r border-sidebar-border overflow-y-auto z-50 flex flex-col transition-colors duration-300 max-lg:hidden">
        <div class="p-5 flex-1 relative z-[1]">
          <a href="/" class="site-title group flex items-center gap-3 no-underline mb-7 p-3 -m-3 rounded-lg transition-all duration-200 hover:bg-muted">
            <span class="site-logo flex items-center justify-center w-9 h-9 rounded-lg bg-primary text-primary-foreground transition-all duration-300">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" class="logo-icon transition-transform duration-300">
                <!-- Ethereum-inspired diamond -->
                <path d="M12 1L3 12l9 5 9-5L12 1z" stroke="currentColor" stroke-width="1.5" fill="none" class="diamond-top transition-opacity duration-300"/>
                <path d="M12 17l-9-5 9 11 9-11-9 5z" stroke="currentColor" stroke-width="1.5" fill="none" class="diamond-bottom"/>
                <path d="M12 1v16" stroke="currentColor" stroke-width="1.5" opacity="0.3"/>
              </svg>
            </span>
            <span class="flex flex-col leading-tight">
              <span class="font-mono text-[0.8125rem] font-bold tracking-wide text-foreground">ETH P2P</span>
              <span class="font-serif text-sm text-muted-foreground -tracking-tight">Notebooks</span>
            </span>
          </a>
          <Sidebar />
        </div>

        <!-- Theme switcher at bottom of sidebar -->
        <div class="p-4 px-5 border-t border-sidebar-border relative z-[1]">
          <div class="theme-switcher flex items-center relative bg-muted rounded-lg p-1" role="radiogroup" aria-label="Theme selection">
            <button
              class="theme-option flex items-center justify-center w-10 h-8 bg-transparent border-none rounded-md text-muted-foreground cursor-pointer transition-all duration-200 relative z-[1] hover:text-foreground [&.active]:text-foreground"
              data-theme="light"
              role="radio"
              aria-checked="false"
              title="Light theme"
            >
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="5"/>
                <line x1="12" y1="1" x2="12" y2="3"/>
                <line x1="12" y1="21" x2="12" y2="23"/>
                <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
                <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
                <line x1="1" y1="12" x2="3" y2="12"/>
                <line x1="21" y1="12" x2="23" y2="12"/>
                <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
                <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
              </svg>
            </button>
            <button
              class="theme-option flex items-center justify-center w-10 h-8 bg-transparent border-none rounded-md text-muted-foreground cursor-pointer transition-all duration-200 relative z-[1] hover:text-foreground [&.active]:text-foreground"
              data-theme="system"
              role="radio"
              aria-checked="true"
              title="System theme"
            >
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect x="2" y="3" width="20" height="14" rx="2" ry="2"/>
                <line x1="8" y1="21" x2="16" y2="21"/>
                <line x1="12" y1="17" x2="12" y2="21"/>
              </svg>
            </button>
            <button
              class="theme-option flex items-center justify-center w-10 h-8 bg-transparent border-none rounded-md text-muted-foreground cursor-pointer transition-all duration-200 relative z-[1] hover:text-foreground [&.active]:text-foreground"
              data-theme="dark"
              role="radio"
              aria-checked="false"
              title="Dark theme"
            >
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
              </svg>
            </button>
            <div class="theme-indicator" id="theme-indicator"></div>
          </div>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="main-content flex-1 ml-64 min-w-0 transition-[margin] duration-300 max-lg:ml-0">
        <div class="max-w-4xl mx-auto py-10 px-12 animate-[fadeUp_0.5s_ease-out] max-lg:p-6">
          <slot />
        </div>
      </main>

      <!-- Right TOC -->
      {showToc && (
        <aside class="right-sidebar fixed top-0 right-0 h-screen w-60 py-10 px-5 overflow-y-auto z-40 max-xl:hidden">
          <TableOfContents />
        </aside>
      )}
    </div>

    <!-- Theme switcher script -->
    <script>
      function initThemeSwitcher() {
        const buttons = document.querySelectorAll('.theme-option');
        const indicator = document.getElementById('theme-indicator');
        const metaThemeColor = document.getElementById('theme-color-meta');

        if (!buttons.length || !indicator) return;

        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');

        function getStoredTheme(): string {
          return localStorage.getItem('theme') || 'system';
        }

        function setTheme(theme: string) {
          localStorage.setItem('theme', theme);
          applyTheme(theme);
          updateIndicator(theme);
          updateAriaStates(theme);
        }

        function applyTheme(theme: string) {
          let isDark: boolean;

          if (theme === 'dark') isDark = true;
          else if (theme === 'light') isDark = false;
          else isDark = prefersDark.matches;

          document.documentElement.classList.toggle('dark', isDark);

          if (metaThemeColor) {
            metaThemeColor.setAttribute('content', isDark ? '#1a1625' : '#f8f7fc');
          }
        }

        function updateIndicator(theme: string) {
          const activeButton = document.querySelector(`[data-theme="${theme}"]`) as HTMLElement;
          if (activeButton && indicator) {
            indicator.style.transform = `translateX(${activeButton.offsetLeft}px)`;
            indicator.style.width = `${activeButton.offsetWidth}px`;
          }
        }

        function updateAriaStates(theme: string) {
          buttons.forEach(btn => {
            const isActive = btn.getAttribute('data-theme') === theme;
            btn.setAttribute('aria-checked', isActive ? 'true' : 'false');
            btn.classList.toggle('active', isActive);
          });
        }

        // Handle button clicks
        buttons.forEach(btn => {
          btn.addEventListener('click', () => {
            const theme = btn.getAttribute('data-theme');
            if (theme) setTheme(theme);
          });
        });

        // Handle system preference changes
        prefersDark.addEventListener('change', () => {
          if (getStoredTheme() === 'system') {
            applyTheme('system');
          }
        });

        // Initialize
        const currentTheme = getStoredTheme();
        applyTheme(currentTheme);

        // Wait for layout to settle before positioning indicator
        requestAnimationFrame(() => {
          updateIndicator(currentTheme);
          updateAriaStates(currentTheme);
        });
      }

      initThemeSwitcher();
      document.addEventListener('astro:page-load', initThemeSwitcher);
    </script>

    <!-- Scroll reveal script -->
    <script>
      function initScrollReveal() {
        const reveals = document.querySelectorAll('.reveal, .reveal-stagger');

        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('visible');
            }
          });
        }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

        reveals.forEach(el => observer.observe(el));
      }

      initScrollReveal();
      document.addEventListener('astro:page-load', initScrollReveal);
    </script>
  </body>
</html>

<style is:global>
  /* Sidebar gradient overlay - needs pseudo-element */
  .left-sidebar::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, transparent 0%, var(--sidebar) 100%);
    opacity: 0.5;
    pointer-events: none;
  }

  /* Logo hover effects */
  .site-title:hover .site-logo {
    transform: scale(1.05);
    box-shadow: var(--glow);
  }

  .site-title:hover .logo-icon {
    transform: rotate(10deg);
  }

  .site-title:hover .diamond-top {
    opacity: 0.7;
  }

  /* Theme indicator positioning - needs JS */
  .theme-indicator {
    position: absolute;
    top: 0.25rem;
    left: 0.25rem;
    height: 2rem;
    background: var(--background);
    border-radius: calc(var(--radius) - 2px);
    box-shadow: 0 1px 3px oklch(0 0 0 / 10%);
    transition: transform 0.25s ease, width 0.25s ease;
    z-index: 0;
  }

  .dark .theme-indicator {
    background: var(--card);
    box-shadow: 0 1px 3px oklch(0 0 0 / 30%);
  }

  /* When TOC is shown, add right margin to prevent overlap */
  @media (min-width: 1280px) {
    .layout-container:has(.right-sidebar) .main-content {
      margin-right: 15rem;
    }
  }
</style>
