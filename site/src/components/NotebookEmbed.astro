---
import fs from 'node:fs';
import path from 'node:path';
import CodeToggle from './CodeToggle.astro';

interface Props {
  htmlPath: string;
  title: string;
}

const { htmlPath, title } = Astro.props;

// Read the pre-rendered HTML at build time
const fullPath = path.join(process.cwd(), 'public', 'rendered', htmlPath);

let content = '';
let error = '';
let headContent = '';

if (fs.existsSync(fullPath)) {
  const html = fs.readFileSync(fullPath, 'utf-8');

  // Extract head content for styles
  const headMatch = html.match(/<head[^>]*>([\s\S]*?)<\/head>/i);
  if (headMatch) {
    // Extract only style tags from head
    const styleMatches = headMatch[1].match(/<style[^>]*>[\s\S]*?<\/style>/gi);
    if (styleMatches) {
      headContent = styleMatches.join('\n');
    }
  }

  // Extract body content from the HTML
  const bodyMatch = html.match(/<body[^>]*>([\s\S]*)<\/body>/i);
  if (bodyMatch) {
    content = bodyMatch[1];

    // Remove Quarto navigation elements we don't need
    content = content
      .replace(/<nav[^>]*class="[^"]*quarto-secondary-nav[^"]*"[^>]*>[\s\S]*?<\/nav>/gi, '')
      .replace(/<header[^>]*id="quarto-header"[^>]*>[\s\S]*?<\/header>/gi, '')
      .replace(/<nav[^>]*id="quarto-sidebar"[^>]*>[\s\S]*?<\/nav>/gi, '')
      .replace(/<footer[^>]*>[\s\S]*?<\/footer>/gi, '');
  } else {
    content = html;
  }
} else {
  error = `Notebook not found: ${htmlPath}`;
}
---

{error ? (
  <div class="error-container">
    <div class="error-icon">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="12" r="10"/>
        <line x1="12" y1="8" x2="12" y2="12"/>
        <line x1="12" y1="16" x2="12.01" y2="16"/>
      </svg>
    </div>
    <h2>Error Loading Notebook</h2>
    <p>{error}</p>
  </div>
) : (
  <>
    <article class="notebook-content">
      <header class="notebook-header">
        <h1 class="notebook-title">{title}</h1>
        <div class="notebook-controls">
          <CodeToggle />
        </div>
      </header>
      <Fragment set:html={headContent} />
      <div class="notebook-body" set:html={content} />
    </article>
  </>
)}

<style is:global>
  .error-container {
    padding: 2.5rem;
    background: oklch(0.95 0.03 25 / 50%);
    border: 1px solid oklch(0.85 0.08 25);
    border-radius: var(--radius);
    color: oklch(0.45 0.15 25);
    text-align: center;
  }

  :global(.dark) .error-container {
    background: oklch(0.20 0.05 25 / 30%);
    border-color: oklch(0.40 0.12 25);
    color: oklch(0.75 0.12 25);
  }

  .error-icon {
    display: inline-flex;
    margin-bottom: 1rem;
    opacity: 0.7;
  }

  .error-container h2 {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    font-weight: 400;
    margin-bottom: 0.5rem;
  }

  .error-container p {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    opacity: 0.8;
  }

  /* Notebook header */
  .notebook-header {
    margin-bottom: 2.5rem;
    padding-bottom: 2rem;
    border-bottom: 1px solid var(--border);
    animation: fadeUp 0.5s ease-out;
  }

  .notebook-title {
    font-family: var(--font-serif);
    font-size: 2.5rem;
    font-weight: 400;
    color: var(--foreground);
    margin: 0 0 1.25rem 0;
    letter-spacing: -0.03em;
    line-height: 1.15;
  }

  :global(.dark) .notebook-title {
    background: linear-gradient(135deg, var(--foreground) 0%, var(--mauve-11) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .notebook-controls {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  /* Section headings */
  .notebook-content h2 {
    font-family: var(--font-serif);
    font-size: 1.625rem;
    font-weight: 400;
    color: var(--foreground);
    margin: 3.5rem 0 1.5rem 0;
    padding-bottom: 0.625rem;
    border-bottom: 1px solid var(--border);
    letter-spacing: -0.02em;
    line-height: 1.25;
    position: relative;
  }

  .notebook-content h2::before {
    content: '';
    position: absolute;
    bottom: -1px;
    left: 0;
    width: 3rem;
    height: 2px;
    background: var(--primary);
  }

  .notebook-content h2:first-of-type {
    margin-top: 0;
  }

  .notebook-content h3 {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    font-weight: 400;
    color: var(--foreground);
    margin: 2.5rem 0 1rem 0;
    letter-spacing: -0.01em;
    line-height: 1.35;
  }

  .notebook-content h4 {
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 600;
    color: var(--foreground);
    margin: 2rem 0 0.75rem 0;
  }

  /* Paragraphs and text */
  .notebook-content p {
    margin: 0 0 1.25rem 0;
    line-height: 1.75;
    color: var(--foreground);
  }

  .notebook-content a {
    color: var(--primary);
    text-decoration: underline;
    text-decoration-color: oklch(0.60 0.15 280 / 40%);
    text-underline-offset: 3px;
    transition: all 0.2s ease;
  }

  .notebook-content a:hover {
    color: var(--accent);
    text-decoration-color: var(--accent);
  }

  /* Lists */
  .notebook-content ul,
  .notebook-content ol {
    margin: 0 0 1.25rem 0;
    padding-left: 1.75rem;
  }

  .notebook-content li {
    margin-bottom: 0.5rem;
    line-height: 1.7;
  }

  .notebook-content li::marker {
    color: var(--primary);
  }

  /* Syntax highlighting - Technical Observatory theme */
  .sourceCode {
    background: var(--muted) !important;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow-x: auto;
    font-size: 0.8125rem;
    line-height: 1.65;
    position: relative;
  }

  :global(.dark) .sourceCode {
    background: oklch(0.14 0.02 260) !important;
    border-color: oklch(0.25 0.02 260);
  }

  .sourceCode pre {
    margin: 0;
    padding: 1.25rem 1.5rem;
    background: transparent !important;
  }

  .sourceCode code {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    background: transparent !important;
    padding: 0;
  }

  /* Token colors - Light mode */
  .sourceCode .kw { color: oklch(0.50 0.20 330); font-weight: 500; } /* Keywords - magenta */
  .sourceCode .dt { color: oklch(0.45 0.18 260); } /* Data types - purple */
  .sourceCode .dv { color: oklch(0.50 0.15 180); } /* Decimal values - teal */
  .sourceCode .bn { color: oklch(0.50 0.15 180); } /* Base N */
  .sourceCode .fl { color: oklch(0.50 0.15 180); } /* Float */
  .sourceCode .ch { color: oklch(0.45 0.12 145); } /* Char - green */
  .sourceCode .st { color: oklch(0.45 0.12 145); } /* String */
  .sourceCode .co { color: var(--muted-foreground); font-style: italic; } /* Comment */
  .sourceCode .ot { color: oklch(0.50 0.20 330); } /* Other */
  .sourceCode .al { color: oklch(0.55 0.20 25); font-weight: bold; } /* Alert - red */
  .sourceCode .fu { color: oklch(0.50 0.18 280); } /* Function - purple */
  .sourceCode .er { color: oklch(0.55 0.20 25); font-weight: bold; } /* Error */
  .sourceCode .wa { color: oklch(0.55 0.15 80); font-style: italic; } /* Warning - gold */
  .sourceCode .cn { color: oklch(0.50 0.15 180); } /* Constant */
  .sourceCode .sc { color: oklch(0.45 0.12 145); } /* Special char */
  .sourceCode .vs { color: oklch(0.45 0.12 145); } /* Verbatim string */
  .sourceCode .ss { color: oklch(0.45 0.12 145); } /* Special string */
  .sourceCode .im { color: oklch(0.50 0.20 330); } /* Import */
  .sourceCode .va { color: var(--foreground); } /* Variable */
  .sourceCode .cf { color: oklch(0.50 0.20 330); font-weight: 500; } /* Control flow */
  .sourceCode .op { color: var(--foreground); } /* Operator */
  .sourceCode .bu { color: oklch(0.45 0.18 260); } /* Built-in */
  .sourceCode .ex { color: oklch(0.50 0.18 280); } /* Extension */
  .sourceCode .pp { color: oklch(0.50 0.20 330); } /* Preprocessor */
  .sourceCode .at { color: oklch(0.45 0.18 260); } /* Attribute */
  .sourceCode .do { color: var(--muted-foreground); font-style: italic; } /* Documentation */
  .sourceCode .an { color: var(--muted-foreground); font-weight: bold; font-style: italic; } /* Annotation */
  .sourceCode .cv { color: var(--muted-foreground); font-weight: bold; font-style: italic; } /* Comment var */
  .sourceCode .in { color: var(--muted-foreground); font-weight: bold; font-style: italic; } /* Information */

  /* Dark mode token colors */
  :global(.dark) .sourceCode .kw { color: oklch(0.75 0.18 330); }
  :global(.dark) .sourceCode .dt { color: oklch(0.72 0.15 260); }
  :global(.dark) .sourceCode .dv { color: oklch(0.75 0.14 180); }
  :global(.dark) .sourceCode .bn { color: oklch(0.75 0.14 180); }
  :global(.dark) .sourceCode .fl { color: oklch(0.75 0.14 180); }
  :global(.dark) .sourceCode .ch { color: oklch(0.72 0.12 145); }
  :global(.dark) .sourceCode .st { color: oklch(0.72 0.12 145); }
  :global(.dark) .sourceCode .fu { color: oklch(0.75 0.15 280); }
  :global(.dark) .sourceCode .al { color: oklch(0.72 0.18 25); }
  :global(.dark) .sourceCode .er { color: oklch(0.72 0.18 25); }
  :global(.dark) .sourceCode .wa { color: oklch(0.78 0.14 80); }
  :global(.dark) .sourceCode .bu { color: oklch(0.72 0.15 260); }
  :global(.dark) .sourceCode .ex { color: oklch(0.75 0.15 280); }

  /* Code folding - refined style */
  .notebook-content details.code-fold {
    margin-bottom: 1.5rem;
  }

  .notebook-content details.code-fold > summary {
    cursor: pointer;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--muted-foreground);
    padding: 0.5rem 0.875rem;
    background: var(--muted);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    transition: all 0.2s ease;
    user-select: none;
    list-style: none;
  }

  .notebook-content details.code-fold > summary:hover {
    color: var(--foreground);
    background: var(--mauve-3);
    border-color: var(--mauve-6);
  }

  .notebook-content details.code-fold > summary::marker,
  .notebook-content details.code-fold > summary::-webkit-details-marker {
    display: none;
  }

  .notebook-content details.code-fold > summary::before {
    content: '\25B6';
    font-size: 0.5rem;
    transition: transform 0.2s ease;
    opacity: 0.6;
  }

  .notebook-content details.code-fold[open] > summary::before {
    transform: rotate(90deg);
  }

  .notebook-content details.code-fold[open] > summary {
    margin-bottom: 1rem;
  }

  /* Charts */
  .notebook-content .js-plotly-plot,
  .notebook-content .plotly-graph-div {
    width: 100% !important;
    margin: 1.5rem 0;
    border-radius: var(--radius);
    overflow: hidden;
  }

  .notebook-content .vega-embed {
    width: 100%;
    margin: 1.5rem 0;
  }

  /* Code cells */
  .notebook-content .cell {
    margin-bottom: 2.5rem;
  }

  .notebook-content .cell-code {
    margin-bottom: 1rem;
  }

  /* Cell output */
  .notebook-content .cell-output-display {
    margin-top: 1.25rem;
  }

  /* Tables */
  .notebook-content table {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5rem 0;
    font-size: 0.875rem;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
  }

  .notebook-content th,
  .notebook-content td {
    padding: 0.75rem 1rem;
    text-align: left;
    border-bottom: 1px solid var(--border);
  }

  .notebook-content th {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: var(--muted);
    color: var(--muted-foreground);
  }

  .notebook-content td {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
  }

  .notebook-content tr:last-child td {
    border-bottom: none;
  }

  .notebook-content tr:hover td {
    background: var(--mauve-2);
  }

  :global(.dark) .notebook-content tr:hover td {
    background: oklch(0.18 0.02 260);
  }

  /* Hide Quarto elements */
  .notebook-content .code-copy-button {
    display: none;
  }

  /* Line numbers */
  .sourceCode pre code span.line-number {
    color: var(--muted-foreground);
    margin-right: 1.25rem;
    user-select: none;
    opacity: 0.5;
  }

  /* Blockquotes */
  .notebook-content blockquote {
    margin: 2rem 0;
    padding: 1.25rem 1.5rem;
    border-left: 3px solid var(--primary);
    background: var(--mauve-2);
    border-radius: 0 var(--radius) var(--radius) 0;
  }

  :global(.dark) .notebook-content blockquote {
    background: oklch(0.16 0.02 260);
  }

  .notebook-content blockquote p:last-child {
    margin-bottom: 0;
  }

  /* Inline code */
  .notebook-content :not(pre) > code {
    background: var(--muted);
    padding: 0.1875rem 0.5rem;
    border-radius: var(--radius);
    font-size: 0.875em;
    font-family: var(--font-mono);
    color: var(--primary);
  }

  :global(.dark) .notebook-content :not(pre) > code {
    background: oklch(0.20 0.02 260);
  }

  /* Images */
  .notebook-content img {
    max-width: 100%;
    height: auto;
    border-radius: var(--radius);
  }

  /* Horizontal rules */
  .notebook-content hr {
    border: none;
    border-top: 1px solid var(--border);
    margin: 3rem 0;
  }

  /* Animation for cells appearing */
  @keyframes cellReveal {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .notebook-content .cell {
    animation: cellReveal 0.5s ease-out backwards;
  }

  .notebook-content .cell:nth-child(1) { animation-delay: 0.1s; }
  .notebook-content .cell:nth-child(2) { animation-delay: 0.15s; }
  .notebook-content .cell:nth-child(3) { animation-delay: 0.2s; }
  .notebook-content .cell:nth-child(4) { animation-delay: 0.25s; }
  .notebook-content .cell:nth-child(5) { animation-delay: 0.3s; }
</style>
